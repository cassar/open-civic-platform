%h1= @issue.name
%p= link_to 'Create New Position', new_issue_position_path(@issue)
- if @positions.present?
  - @positions.each do |position|
    %h2= position.name
    %p= position.outline
- else
  %p This issue does not have any positions declared.
%p= link_to 'Back to Group', group_path(@group )
